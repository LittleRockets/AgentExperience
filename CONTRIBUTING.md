# Contributing

AgentExperience is pre-alpha. Changes must preserve the event log's recovery guarantees,
include tests, and avoid adding framework dependencies to the core installation.

Use `python -m pip install -e ".[dev]"` for development and `python -m build` for releases.
Do not invoke `setup.py` directly.

